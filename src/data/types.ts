/* 資料交換層型別 — 完整版由 Task 3 建立（src/data/types.ts 屆時會補齊
   overview/policy/dispatch/epidemic/alert/carbon/twin 各 Provider 欄位）。
   本檔是 Task 2 的最小 stub：screens/types.ts 的 ScreenCtx.data 型別參照
   （`import('../data/types').DataExchange`）需要這個模組存在才能通過型別檢查，
   main.ts 目前以 `{} as any` 佔位，尚未依賴這裡的實際欄位形狀。 */
export interface DataExchange {}
